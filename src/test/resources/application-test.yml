datasources:
  default:
    url: jdbc:tc:mysql:8.0:///devDb
    driverClassName: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: test
    password: test

jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: create-drop
        show_sql: true